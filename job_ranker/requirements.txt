# Job Ranker Requirements
# Python 3.10+

# Core dependencies
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
pydantic>=2.5.0
numpy>=1.24.0

# Optional for better models (will be downloaded from HuggingFace on first use)
# torch>=2.0.0  # Usually installed with sentence-transformers
# transformers>=4.30.0  # Usually installed with sentence-transformers

# Testing
pytest>=7.4.0

# Note: First run will download models from HuggingFace:
# - all-MiniLM-L6-v2 (~80MB) for embeddings
# - cross-encoder/ms-marco-MiniLM-L-6-v2 (~80MB) for re-ranking
